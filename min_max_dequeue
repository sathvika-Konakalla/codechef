for i in range(int(input())):
    n,q=map(int,input().split())
    arr=list(map(int,input().split()))
    b=(arr[0],arr[1])
    for i in range(2,n):
        if i%2==0:
            b=(arr[i],max(b))
        else:
            b=(arr[i],min(b))
    print(min(b))   
