t=int(input())
for i in range(t):
    n=int(input())
    cost=float('inf')
    for j in range(n):
        a,b=map(int,input().split())
        if a>=7:
            cost=min(cost,b)
    print(cost if cost!=float('inf') else -1)
