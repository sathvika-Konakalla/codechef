# cook your dish here
T=int(input())  
for i in range(T):
    N,A,B=map(int, input().split())
    dista=float('inf')  
    for j in range(N):
        X, Y=map(int, input().split())
        dist=abs(A - X) + abs(B - Y)  
        dista=min(dista,dist)

    print(dista)
