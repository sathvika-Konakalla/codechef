# cook your dish here
import math
for i in range(int(input())):
    a,b = map(int,input().split())
    if math.gcd(a,b) > 1:
        print(0)
    else:
        if math.gcd(a + 1,b) > 1 or math.gcd(a,b + 1) > 1:
            print(1)
        else:
            print(2)
            
